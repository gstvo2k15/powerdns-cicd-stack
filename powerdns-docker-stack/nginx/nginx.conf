http {
    server {
        listen 80;
        server_name ${DUCKDNS_DOMAIN};

        location / {
            proxy_pass http://powerdns:8081;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
